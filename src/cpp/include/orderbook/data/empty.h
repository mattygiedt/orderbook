#pragma once

/**
 * This is an empty struct, used to initialize the EventData variant, as its
 * default constructor is hidden.
 */

namespace orderbook::data {
struct Empty {};
}  // namespace orderbook::data
